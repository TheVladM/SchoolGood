<div class="form-field"><label class="label" for="name">{{ __('rooms.form_name') }}</label><input id="name" name="name" class="field" required value="{{ old('name', $room->name ?? '') }}"></div>
<div class="form-field"><label class="label" for="building">{{ __('rooms.form_building') }}</label><input id="building" name="building" class="field" value="{{ old('building', $room->building ?? '') }}"></div>
<div class="form-field"><label class="label" for="floor">{{ __('rooms.form_floor') }}</label><input id="floor" name="floor" class="field" value="{{ old('floor', $room->floor ?? '') }}"></div>
<div class="form-field"><label class="label" for="capacity">{{ __('rooms.form_capacity') }}</label><input id="capacity" name="capacity" type="number" min="1" class="field" value="{{ old('capacity', $room->capacity ?? '') }}"></div>
